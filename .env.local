# PulseCore frontend – copy to .env.local and set your backend URL
# Leave empty or omit to use mock data only.
# After setting this, restart the frontend (npm run dev or start-frontend.cmd).
NEXT_PUBLIC_API_URL=http://localhost:5000
